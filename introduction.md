#### 数据结构基础
##### 定义  

相互之间存在一种或多种特定的关系的数据元素集合.

##### 数据结构的三要素
数据结构主要包含三方面的内容:
1. 逻辑结构
逻辑结构指数据之间的逻辑关系, 与数据的存储无关, 是独立于计算机的.
数据的逻辑结构分为两大类:
    - 线性结构: 一般线性表, 栈, 队列, 数组等
    - 非线性结构: 树(一般树, 二叉树), 图(有向图, 无向图)
2. 存储结构
存储结构, 又称为物理结构,指数据在在计算机中的实际表示方式.依赖于计算机.
主要有:
    - 顺序存储: 在逻辑上相邻的节点在物理位置上也是相邻的存储节点(随机存取, 要求整块存储单元, 易产生外部碎片)
    - 链接存储: 不要求逻辑上相邻的节点在物理上也相邻.(顺序存取, 不能随机存取, 充分利用内存单元, 每个节点占用更多内存)
    - 索引存储: 建立存储节点的索引表(检索速度快, 耗费内存)
    - 散列存储: 根据关键字通过散列函数来计算出节点的存储地址(检索快, 散列函数易导致存储单元冲突)
3. 数据运算
运算包括:运算的定义和实现, 运算定义主要针对逻辑结构, 运算的实现,针对的是存储结构.

##### 算法
算法是对特定问题求解步骤的一种描述, 它是指令的有限序列.

1. 算法的五个特性
    - 有穷性
    - 确定性
    - 可行性
    - 输入性
    - 输出性
2. 算法评价
    - 时间复杂度:  
    一个语句的频度是指该语句在算法中被重复执行的次数.算法中的所有语句的频度之和记为T(n), 它是问题规模n的函数, 时间复杂度主要分析T(n)的数量级.
    简单的说就是执行最基础语句的次数之和的数量级.
    
    - 最坏时间复杂度, 平均时间复杂度, 最好时间复杂度.  
    一般总是考虑最坏情况的时间复杂度.
    - 空间复杂度  
    算法的空间复杂度S(n), 定义为算法耗费的存储空间, 它是问题规模n的函数
    算法原地工作是指算法所需辅助空间是常量, 即O(1)
    
##### 章节

数据结构介绍分为:

- 线性表: data-structure-LinearList  
- 栈,队列和数组: data-structure-Stack&Queue&Array
- 树和二叉树: data-structure-Tree
- 查找: data-structure-Search
- 排序: data-structure-Sort
    
date: 2019.7.20    